---
stt: 029
timestamp: 2026-08-27T22:58+07:00
model: claude-opus-5
scope: [frontend/src/lib/error-code.ts, frontend/src/components, frontend/src/app/(app)]
---

## Prompt
Thay từ "quý vị" thành từ "bạn" hết

## Kết quả

Đổi lại đại từ xưng hô từ "Quý vị" về "bạn" ở toàn bộ chuỗi hiển thị — **giữ nguyên cấu trúc
câu lịch sự** đã viết ở dev-log 028 ("vui lòng…", "Rất tiếc…", "Thành thật xin lỗi…",
"hệ thống xin phép…", "rất mong … thông cảm"). Chỉ đại từ đổi, giọng văn không lùi về mức
thân mật cũ ("Đăng nhập lại nhé", "Thử lại nhé" **không** quay lại).

Phạm vi: 13 file trong `src/lib`, `src/components`, `src/app/(app)` — không còn chuỗi "Quý vị"
nào trong repo.

**Xử lý viết hoa theo vị trí trong câu**, không thay máy móc:

- Đầu câu / đầu chuỗi / `title=` / `question=` / `<DialogTitle>` → **"Bạn"**
  (ví dụ `'Phiên đăng nhập đã hết hạn. Bạn vui lòng đăng nhập lại.'`)
- Giữa câu → **"bạn"**
  (ví dụ `'…không tìm thấy nội dung bạn yêu cầu.'`, `'…lựa chọn của bạn.'`)
- Một chỗ cố ý giữ chữ thường vì đứng sau dấu gạch ngang giữa câu:
  `step-4.tsx:267` — *"Hệ thống không tự sửa gì — bạn là người quyết định cuối cùng."*
- `versions/page.tsx:184` giữ nguyên `'bạn'` chữ thường: đó là **giá trị nhãn** render giữa câu
  ("do kịch bản" / "do bạn"), không phải đầu câu.

Không đụng backend, `prompts/`, animation/token của dev-log 027–028.

`tsc --noEmit` exit 0 · `eslint` exit 0 · `vitest run` 8 file / 32 test pass.
Bỏ qua `next build` vòng này: thay đổi thuần chuỗi văn bản (TypeScript đã được `tsc` kiểm),
và dev server của người dùng đang chạy trên `.next` — không chạy build để khỏi đạp lên nó.

Đã ghi một dòng đính chính vào cuối dev-log 028 để bản ghi đó không mô tả sai trạng thái hiện tại.
