---
stt: 035
timestamp: 2026-08-30T09:56+07:00
model: claude-opus-5
scope:
  [
    backend/src/critique,
    backend/prisma/schema.prisma,
    backend/src/app.module.ts,
    backend/eval/seed-ambiguity-demo.ts,
  ]
---

## Prompt
start #12

## Kết quả
Hiện thực #12 trên branch `b6/ambiguous-detector`: module `critique/` mới, phát hiện thuần 0
token với 4 loại cờ, `AmbiguityFlag` + `Project.ambiguity_detector` (mặc định tắt), hạn mức 4
câu hỏi mở dùng chung slot với analyze. Đọc lại #12 và thu hẹp phạm vi: trường **rỗng** đã là
việc của `MISSING` do generator gán, nên `AMBIGUOUS` chỉ dành cho trường **có chữ mà không đo
được** — không ghi đè `MISSING`. 173/173 test xanh; chạy thật 7 thẻ → 5 cờ, quét 3 lần không
trôi trạng thái. Tạo thêm issue #25 (đã đăng nhập vẫn vào được `/login`).
