---
stt: 045
timestamp: 2026-08-31T18:24+07:00
model: claude-opus-5
scope: [frontend/src/components/concept-map.tsx, frontend/src/components/steps/step-1.tsx]
---

## Prompt
tiếp tục quay lại main và impl issue kế tiếp

## Kết quả

Issue **#14 · C1 · Concept map của ý tưởng ở bước 1** — task thứ ba của làn C. Nhánh
`c1/concept-map`, đã rebase lên `main` sau khi PR #30 (#17) merge.

**`frontend/src/components/concept-map.tsx` (mới)** — SVG ba tầng: ý tưởng → nhóm loại thẻ → thẻ.
Màu vạch trạng thái lấy từ cùng bảng của `status-style.ts`. Bấm một nút mở hộp sửa tiêu đề ·
nội dung · trạng thái qua `PATCH /cards/:id` (endpoint **đã có**, không thêm cái nào mới). Hiệu ứng
dựng dần so le: tâm → nhóm (60ms) → thẻ (45ms), tắt sạch dưới `prefers-reduced-motion`.

**`step-1.tsx`** — thêm `ViewToggle`: Bản đồ ⇄ Bảng thẻ. `CardBoard` **giữ nguyên**, chỉ chuyển
sang tab thứ hai đúng như issue yêu cầu ("không xoá thứ đang chạy"). Diff là +11/−1, và dòng bị
thay thế vẫn được render trong nhánh `board`.

### Bốn quyết định

1. **Cạnh là cạnh CẤU TRÚC, không phải quan hệ ngữ nghĩa.** `generatedCardSchema` không có trường
   quan hệ nào và `Card.parent_card_id` tuy có cột nhưng không dòng code nào ghi vào. Vẽ cạnh ngữ
   nghĩa mà dữ liệu không có là bịa ra quan hệ — có `HintBox` nói thẳng điều đó với người dùng.
2. **Bố cục là hàm thuần của dữ liệu**, không dùng force-directed: thuật toán lực có yếu tố ngẫu
   nhiên nên mỗi lần mở ra một hình khác. Với công cụ nghiên cứu thì "cùng dữ liệu, cùng hình"
   đáng giá hơn hình đẹp. Có test render hai lần rồi so toàn bộ toạ độ.
3. **`STATUS_FILL` phải khai literal.** `status-style.ts` cho class nền (`bg-ok-ink`), SVG cần
   `fill-*`. Định dùng `.replace('bg-','fill-')` cho một nguồn sự thật, nhưng Tailwind quét mã
   nguồn tìm tên class **dạng literal** — chuỗi sinh lúc chạy sẽ không có trong CSS. Buộc khai
   lại, kèm cảnh báo đồng bộ.
4. **Không dùng `Tabs` của shadcn** — nó đã bị xoá ở đợt revamp (0 chỗ dùng), và `components/ui/**`
   nằm ngoài phạm vi #14. Hai nút `ViewToggle` có `aria-pressed` là đủ cho hai lựa chọn.

### Một chỗ tự sửa sau khi test bắt

Ban đầu cắt nhãn loại thẻ ở 18 ký tự → "Khoảng trống nghi…". Test fail và nó đúng: **tiêu đề thẻ**
là câu tự do, cắt vẫn đoán được; **nhãn loại thẻ** là tên phân loại, cắt là mất nghĩa. Nới ô nhóm
124 → 156px cho vừa nhãn dài nhất thay vì cắt chữ.

### ❌ Một gạch đầu dòng KHÔNG làm được

Issue yêu cầu *"kéo một nút ra để bỏ"*. Không có `DELETE /cards/:id` — `spec.controller.ts` chỉ có
`@Get('spec-versions/:id/cards')` và `@Patch('cards/:id')`; `card.deleteMany` chỉ tồn tại nội bộ
trong `generator.service.ts` (xoá-rồi-sinh-lại). Và không `CardStatus` nào trong 6 giá trị mang
nghĩa "đã bỏ".

**Không ship nút xoá giả**: một nút "bỏ" chỉ ẩn khỏi màn hình mà không đổi dữ liệu là nói dối bằng
giao diện. Phần này để trống và báo lên GitHub cùng chỗ thiếu tương tự của #15
(`POST /cards/:id/sources`, `DELETE /card-sources/:id`).

### Test — 7 case mới

Nút bấm có nhãn cho trình đọc màn hình · chỉ vẽ nhóm cho loại thẻ thực sự có mặt · **bố cục tất
định** · bấm nút mở đúng hộp sửa · không có thẻ thì nói rõ chứ không vẽ hình rỗng · `aria-pressed`
trên `ViewToggle` · `onChange` đúng chế độ.

**Không đụng backend gì cả.**

Đã rebase lên `main` (mang về PR #30 đã merge, module `critique/` của B6, migration
`b_ambiguity_flag`). Không conflict.

FE `tsc` 0 · `eslint` 0 · `next build` 0 · `vitest` **46/46** · BE `jest` **200/200** (không đụng
nhưng chạy để chắc rebase không làm vỡ gì).
