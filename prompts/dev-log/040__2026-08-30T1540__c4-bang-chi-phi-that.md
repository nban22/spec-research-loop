---
stt: 040
timestamp: 2026-08-30T15:40+07:00
model: claude-opus-5
scope: [backend/src/analytics, frontend/src/app/(app)/projects/[id]/cost, frontend/src/components/command-palette.tsx]
---

## Prompt
Hãy làm theo thứ tự đề xuất, dựa theo phần mô tả của issue đó. Xong mỗi issue review lại cho tôi trước khi commit

## Kết quả

Issue **#17 · C4 · Bảng theo dõi token, thời gian và chi phí thật** — task đầu tiên của làn C
theo thứ tự epic #23 đã chốt ("làm đầu tiên").

**Backend — `src/analytics/` (mới, chỉ đọc)**

- `analytics.service.ts` · `costOverview(projectId, userId)` gom `LlmCall` theo **ba lát cắt**:
  bước (`purpose` → B1…B5), `prompt_id`, `model`. Kèm tỉ lệ ăn cache prefix, tỉ lệ thử lại,
  tỉ lệ lời gọi hỏng, và đối chiếu `ResourceEstimate.cost_usd` với chi phí thật.
- `analytics.controller.ts` chỉ có `@Get`. `analytics.module.ts` **không** `@Global()`.
- Đăng ký `AnalyticsModule` trong `app.module.ts`.

**Frontend — route `/projects/[id]/cost`**

Bốn ô tổng quan + bốn ô độ tin cậy + khối "ước lượng vs thực tế" + ba bảng lát cắt. Dùng lại
`Panel`, `StatTileGrid`, `HintBox`, `EmptyState`, `TableSkeleton`, `StatTileSkeleton`. Dưới `md`
bảng đổi sang danh sách. Thêm nhóm lệnh **"Chi phí"** vào `command-palette.tsx` làm đường vào —
`top-nav.tsx` nằm ngoài phạm vi sở hữu của làn C.

### Bốn quyết định đã trình bày và được duyệt

1. **Đơn giá chép lại** từ `estimator.service.ts` (hai hằng số ở đó không export, và
   `backend/src/estimator/**` là vùng cấm sửa của issue). Cố ý dùng **cùng một đơn giá** cho cả
   hai vế: màn hình này đo *sai số của phép ước lượng*, tính chi phí thật bằng đơn giá khác thì
   con số chênh lệch đo lẫn cả chênh giá. Có ghi cảnh báo "sửa một chỗ thì sửa cả hai".
2. **Không thêm cờ trên `Project`** dù ràng buộc chung của nhóm yêu cầu. Đây là route `GET`
   chỉ đọc trên URL mới — không có gì để làm hỏng demo người khác, không có gì để ablate. Đổi
   lại tránh được một migration, tài nguyên tranh chấp nặng nhất giữa ba làn.
3. **Đường vào bằng bảng lệnh** thay vì nav, để không chạm `top-nav.tsx`.
4. **Bảng theo bước luôn sắp B1→B5**, không sắp theo tiền — có test khoá lại. Sắp theo tiền thì
   bảng nhảy thứ tự giữa hai lần xem.

### Một cảnh báo viết thẳng lên UI

`ResourceEstimate.cost_usd` là dự toán cho **thí nghiệm sắp chạy**; tổng `LlmCall` là tiền đã
tiêu để **dựng bản đặc tả**. Hai đại lượng khác loại. Để bốn ô số cạnh nhau mà không nói gì thì
người đọc sẽ trừ chúng rồi kết luận sai, nên có `HintBox` tông `warn` giải thích đây là thước đo
mức lạc quan của bộ ước lượng.

### Test — 9 case mới

Mock Prisma **ném lỗi nếu ai gọi `create`/`update`/`delete`**: ràng buộc "không ghi DB" của issue
được khoá bằng test chứ không chỉ bằng lời hứa. Kèm các case: đúng công thức đơn giá · giữ thứ tự
B1→B5 · đếm retry/hỏng · tỉ lệ cache · mẫu số 0 trả `null` chứ không `NaN`/`Infinity` · dự toán
bằng 0 trả `null` chứ không `Infinity` · dự án người khác trả `notFound` · mọi truy vấn mang `user_id`.

**Không đụng:** `backend/src/llm/**`, `backend/src/estimator/**`, `prisma/schema.prisma`,
`frontend/src/components/ui/**`, `wizard-shell.tsx`, `status-style.ts`.

`eslint` BE 0 · `nest build` 0 · `jest` **160/160** · FE `tsc` 0 · `eslint` 0 · `next build` 0
(route `ƒ /projects/[id]/cost` có trong bảng route).
